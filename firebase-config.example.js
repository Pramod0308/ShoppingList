// Fill this with your Firebase web config (Console → Project settings → General → Your apps → Web app).
// Copy this file to firebase-config.js and replace the placeholder values with your project's config.
// WARNING: firebase-config.js contains secrets and is gitignored; do not commit real credentials.
export const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_AUTH_DOMAIN",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_STORAGE_BUCKET",
  messagingSenderId: "YOUR_MESSAGING_SENDER_ID",
  appId: "YOUR_APP_ID",
  measurementId: "YOUR_MEASUREMENT_ID"
};
